name: nexus_bridge_adapter
description: WebSocket client for the Nexus Bridge companion service.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  unification:
    path: ../unification
  web_socket_channel: ^3.0.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
