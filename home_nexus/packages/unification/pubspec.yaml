name: unification
description: Core data model - UniversalDevice, Capability, DeviceOrigin. Zero dependencies.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
