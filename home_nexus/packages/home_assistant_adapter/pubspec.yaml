name: home_assistant_adapter
description: Home Assistant WebSocket adapter - maps HA entities to UniversalDevice.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  unification:
    path: ../unification
  web_socket_channel: ^3.0.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
